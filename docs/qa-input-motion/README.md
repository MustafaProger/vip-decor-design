# Подбор, контакты, поля и анимации — 16 сентября 2026

- В подборе восстановлены соединительные линии на мобильных экранах. Пройденные участки плавно окрашиваются; смена комнаты растворяет фотографии, смена шага мягко проявляет содержимое без перемонтирования формы.
- Из контактов удалён блок «Ткани вживую / Советы дизайнера / Отзывы клиентов». Добавлено однократное появление двух основных блоков через существующий Reveal.
- Общий минимум размера шрифта для полей ввода, textarea и select — 16 px, включая динамические страницы и диалоги.
- Используется уже установленный Framer Motion; новые анимации учитывают prefers-reduced-motion.

## Проверка

`npm run build` и `git diff --check` пройдены. Vite сообщает предупреждение о главном JS-файле более 500 kB.

Chrome, локальный http://127.0.0.1:5180, отдельные временные профили:

- 320, 390, 700, 768, 1440 px: три видимые линии; выбор комнаты; проверка обязательного материала; размеры; контакты; возврат назад с сохранением имени и размеров; заполнение линий прогресса.
- Те же размеры: контакты без удалённого блока; модальное окно заявки; каталог; калькулятор; пустая корзина. Проверены вычисленные размеры шрифтов и отсутствие горизонтального переполнения.
- 320, 390, 1440 px: товар, добавление в локальную корзину и открытие формы оформления; размеры полей и отсутствие переполнения.
- Режим уменьшения движения: шаг меняется без сдвига содержимого; пройденная линия видна.
- Ошибок JavaScript в основном прогоне: 0. Отправка заявок и заказов не выполнялась.

Детали: results.json (41 проверка), commerce-fields.json (6 проверок). Скриншоты: selection-390/1440.png, contacts-390/1440.png. Физический iPhone не проверялся; минимальный размер шрифта подтверждён через computed styles.
