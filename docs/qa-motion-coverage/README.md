# Расширение Framer Motion — 16 сентября 2026

Анимации теперь видны на главной (первый экран, шаги, категории, приглашение в шоурум), в каталоге и избранном (общие карточки товаров), в товаре (фото и информация), в отзывах (заголовок, статистика, карточки), в прайсе (разделы цен). Общая анимация Reveal также обновляет существующие блоки компании, услуг и контактов.

Появление: смещение 28 px и прозрачность, 650 мс; последовательная задержка карточек 70–100 мс. Каждый блок появляется один раз. Повторная прокрутка не скрывает содержимое. Фокус внутри блока показывает его без задержки. При prefers-reduced-motion содержимое сразу видно без движения. Формы не перемонтируются.

Проверки:
- npm run build и git diff --check — успешно. Сохраняется предупреждение Vite о главном пакете более 500 kB.
- Chrome, 390 и 1440 px: /, /catalog, /product/325186268288, /projects, /price, /contacts, /company.
- На всех 14 сочетаниях страницы и ширины зафиксированы промежуточные значения прозрачности и transform реальных элементов каждые 30 мс. Итоговые состояния: opacity 1, transform none. Данные в results.json.
- Главная: появление карточки после прокрутки, отсутствие повторного скрытия, раскрытие при фокусе с клавиатуры.
- Каталог: поиск с пустым результатом и восстановление карточек после очистки поля.
- Уменьшение движения: все блоки главной, включая находящиеся ниже экрана, видимы без transform.
- Ошибок JavaScript и горизонтального переполнения нет.
- Дополнительно: товар, добавление в локальную корзину и открытие оформления на 320, 390, 1440 px; поля не меньше 16 px. Отправка заявок не выполнялась.
